'use strict'

exports.__esModule = true
exports.default = void 0

var _useStyles = require('./useStyles')

var _default = (name, styles) => {
  let hook = props => (0, _useStyles.useStyles)(name, styles, props)

  function _ref(props) {
    return (0, _useStyles.useStyles)(styles, props)
  }

  if (typeof name === 'object' && name !== null) {
    styles = name
    name = void 0
    hook = _ref
  }

  if (process.env.NODE_ENV !== 'production')
    Object.defineProperty(hook, 'name', {
      value: name
        ? `use${name.charAt(0).toUpperCase()}${name.slice(1)}`
        : `useStyleHook({${Object.keys(styles).join(', ')}})`,
    })
  return hook
}

exports.default = _default
