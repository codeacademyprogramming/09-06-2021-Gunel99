'use strict'

exports.__esModule = true
exports.__buttonStyles = exports.size = void 0

var _utils = require('../utils')

var dT = _interopRequireWildcard(require('./defaultTheme'))

function _interopRequireWildcard(obj) {
  if (obj && obj.__esModule) {
    return obj
  } else {
    var newObj = {}
    if (obj != null) {
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc =
            Object.defineProperty && Object.getOwnPropertyDescriptor
              ? Object.getOwnPropertyDescriptor(obj, key)
              : {}
          if (desc.get || desc.set) {
            Object.defineProperty(newObj, key, desc)
          } else {
            newObj[key] = obj[key]
          }
        }
      }
    }
    newObj.default = obj
    return newObj
  }
}

const size = (0, _utils.memoTheme)((s, t) => {
    const value = (0, _utils.get)(t.button, 'scale', dT)[s]
    return typeof value === 'function' ? value(t) : value
  }),
  __buttonStyles = (v, t, p) => [
    (0, _utils.get)(t.button, 'getHoverClass', dT)(t, p),
    (0, _utils.get)(t.button, 'getActiveClass', dT)(t, p),
  ]

exports.__buttonStyles = __buttonStyles
exports.size = size
