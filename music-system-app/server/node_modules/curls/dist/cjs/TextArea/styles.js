'use strict'

exports.__esModule = true
exports.__textAreaStyles = exports.autoResize = void 0

var _core = require('@emotion/core')

var _utils = require('../utils')

var dT = _interopRequireWildcard(require('./defaultTheme'))

function _interopRequireWildcard(obj) {
  if (obj && obj.__esModule) {
    return obj
  } else {
    var newObj = {}
    if (obj != null) {
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc =
            Object.defineProperty && Object.getOwnPropertyDescriptor
              ? Object.getOwnPropertyDescriptor(obj, key)
              : {}
          if (desc.get || desc.set) {
            Object.defineProperty(newObj, key, desc)
          } else {
            newObj[key] = obj[key]
          }
        }
      }
    }
    newObj.default = obj
    return newObj
  }
}

const autoResize = {
  name: '1lr9k56',
  styles:
    'display:block !important;overflow:hidden !important;resize:none !important;',
}
exports.autoResize = autoResize

const __textAreaStyles = (_, t, p) => [
  (0, _utils.placeholder)(
    (0, _utils.get)(t.textArea, 'getPlaceholderClass', dT)(t, p)
  ),
  (0, _utils.get)(t.textArea, 'getHoverClass', dT)(t, p),
  (0, _utils.get)(t.textArea, 'getFocusClass', dT)(t, p),
]

exports.__textAreaStyles = __textAreaStyles
