'use strict'

exports.__esModule = true
exports.Drop = exports.useDrop = void 0

var _core = require('@style-hooks/core')

var _createRenderProp = _interopRequireDefault(require('../createRenderProp'))

var _useToggleVisibility = _interopRequireDefault(
  require('../useToggleVisibility')
)

var _utils = require('../utils')

var styles = _interopRequireWildcard(require('./styles'))

function _interopRequireWildcard(obj) {
  if (obj && obj.__esModule) {
    return obj
  } else {
    var newObj = {}
    if (obj != null) {
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc =
            Object.defineProperty && Object.getOwnPropertyDescriptor
              ? Object.getOwnPropertyDescriptor(obj, key)
              : {}
          if (desc.get || desc.set) {
            Object.defineProperty(newObj, key, desc)
          } else {
            newObj[key] = obj[key]
          }
        }
      }
    }
    newObj.default = obj
    return newObj
  }
}

function _interopRequireDefault(obj) {
  return obj && obj.__esModule ? obj : {default: obj}
}

function _ref(props) {
  return (0, _core.useStyles)('drop', styles, props)
}

const useDrop = props =>
    (0, _useToggleVisibility.default)(
      _ref,
      (0, _utils.assignDefaults)(
        {
          property: 'visibility, transform, opacity',
          fromBottom: true,
        },
        props
      )
    ),
  Drop = (0, _createRenderProp.default)(useDrop)

exports.Drop = Drop
exports.useDrop = useDrop

if (process.env.NODE_ENV !== 'production') {
  const propTypes = require('../Slide/propTypes').default

  Drop.displayName = 'Drop'
  Drop.propTypes = propTypes
}
