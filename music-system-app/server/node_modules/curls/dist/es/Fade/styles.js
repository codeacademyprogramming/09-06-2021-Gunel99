import {css} from '@emotion/core'
export const from = () => null
export const to = () => null
export const baseIsNotVisible = {
  name: 'gyp8mm',
  styles: 'visibility:hidden;',
}
export const baseIsVisible = {
  name: 'rrm59m',
  styles: 'visibility:visible;',
}
export const isVisible_ =
  /*#__PURE__*/
  css(baseIsVisible, ';opacity:1;')
export const isNotVisible_ =
  /*#__PURE__*/
  css(baseIsNotVisible, ';opacity:0;')
export const isVisible = (value, _, props) =>
  value === true
    ? props.to === 1
      ? isVisible_
      : /*#__PURE__*/
        css(baseIsVisible, ';opacity:', props.to, ';')
    : props.from === 0
    ? isNotVisible_
    : /*#__PURE__*/
      css(baseIsNotVisible, ';opacity:', props.from, ';')
